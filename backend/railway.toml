[deploy]
restartPolicyType = "on_failure"
startCommand = "python manage.py check 2>&1 && python manage.py collectstatic --no-input 2>&1 && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --access-logfile -"
healthcheckPath = "/health/"
healthcheckTimeout = 300
