[build]
buildCommand = "apt-get update && apt-get install -y libcairo2-dev libffi-dev pkg-config && python -m pip install -r ./requirements.txt"

[deploy]
startCommand = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
restartPolicyType = "on_failure"
