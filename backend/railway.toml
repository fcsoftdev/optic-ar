[deploy]
restartPolicyType = "on_failure"
startCommand = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --preload --log-level debug --access-logfile - --error-logfile -"
healthcheckPath = "/health/"
healthcheckTimeout = 300
