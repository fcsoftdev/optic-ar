# En desarrollo, dejar vacío para usar el proxy de Vite (vite.config.ts).
# El proxy redirige /api/* → http://127.0.0.1:8000 sin problemas de SameSite.
# En producción, setear la URL completa del backend.
VITE_API_URL=

